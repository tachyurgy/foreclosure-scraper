# Foreclosure Scraper Configuration
# Copy this file to .env and adjust values as needed

# Scheduling (days between automated runs)
SCHEDULE_INTERVAL_DAYS=14

# Rate limiting (requests per second - lower is safer)
REQUESTS_PER_SECOND=0.5

# Export format (csv, xlsx, json)
EXPORT_FORMAT=csv
